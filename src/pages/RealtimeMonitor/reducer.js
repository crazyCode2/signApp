import  { realtimeMonitorAction } from '../../store/rootAction';

let stateInit = {
  //
};

function realtimeMonitorReducer(state=stateInit, action) {
  return state;
}

export default realtimeMonitorReducer;