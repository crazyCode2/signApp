import { fork ,put, call, take, takeEvery,select } from 'redux-saga/effects';
import { myElectricityAction } from '../../../store/rootAction';

export default function * myElectricitySaga(){
  //
}